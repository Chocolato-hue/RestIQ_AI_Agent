# Package Manager Preference

- Always prefer `bun` instead of `npm` for installing dependencies, running scripts, and building projects.
- Always prefer `bunx` instead of `npx` for executing binaries or scaffolding scripts.
- Only fallback to `npm` or `npx` if `bun` is not available on the host system or if explicitly requested.
